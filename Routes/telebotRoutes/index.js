const express = require("express");
const router = express.Router();
const { botController } = require("../../controllers/telebotController/index");

// router.post("/", botController);

module.exports = router;
